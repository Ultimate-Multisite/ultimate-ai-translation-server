{
  "name": "gratis-ai-translations-server",
  "version": "1.3.0",
  "private": true,
  "description": "Server-side WordPress plugin for serving AI-generated plugin and theme translations via REST API.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "composer archive --file=$npm_package_name --format=zip"
  }
}
